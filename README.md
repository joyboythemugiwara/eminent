# Project

This repository contains a backend and frontend application.

## Structure

- `backend/` - API, database, middleware, and tests
- `frontend/` - Next.js application, components, and UI

## Notes

- `frontend` is tracked as a normal directory in this repo
- Backend environment should be configured with `.env`
- Use `git status` to verify tracked changes before pushing

